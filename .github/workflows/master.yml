name: Master build

on:
  pull_request: {}
  push:
    branches:
      - master
      
      
 jobs:
   build:
      runs-on: windows-latest
      steps:
        - name: Install Go
          uses: actions/setup-go@v2
          with:
             go-version: 1.15.x
        - name: Checkout
          uses: actions/checkout@v2
